# Task Manager Frontend

This is the React frontend for the Task Manager app.

## Setup

1. Clone the repo
2. Run `npm install` to install dependencies
3. Create a `.env` file with your API URL
4. Run `npm run dev` to start the development server

## Deployment

Built with Vite + React. Can be deployed on Render or other hosting platforms.